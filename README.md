
  # Interactive SVG Map

  This is a code bundle for Interactive SVG Map. The original project is available at https://www.figma.com/design/K5h12AH2hwsPWIT9sFL0be/Interactive-SVG-Map.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  